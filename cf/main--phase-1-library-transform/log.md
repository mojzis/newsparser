# Log: phase-1-library-transform

- **Plan source**: ./plan.md
- **Branch**: main
- **Base commit**: 6133c8d9d9353ed4729d655bc890c97d4a577b5e
- **Phases**: 1
- **Review skill**: python-review
- **Models**: orchestrator=haiku, planner=opus, dev=sonnet, check=sonnet, review=opus
- **Skipped steps**: none

## Bootstrap
- 2026-07-05T22:20:47+02:00 · plan approved, execution starting

## Phase 1 — dev
- 2026-07-05T22:30:00+02:00
```json
{"commit_shas": ["18c228b"], "summary": "Genericized the MCP topic into a config/base/app.yaml `topic` section used by the prompt template, and renamed is_mcp_related to is_relevant across models, evaluator, stages, CLI, and tests with backward-compat fallback reads for old data.", "deviations": ["Left src/html/duckdb-query-tool*.html references to is_mcp_related untouched — those files aren't in the brief's in-scope list and query historical parquet/frontmatter data that may still use the old column name.", "Applied 3 minor python-review cleanups beyond the literal brief text (deduped the is_relevant/is_mcp_related fallback into a private _is_relevant() helper in src/stages/report.py, fixed a stale 'MCP relevance score' docstring in src/models/url_registry.py, and added a comment explaining the anthropic_client is_mcp_related fallback) — all within already-in-scope files and consistent with the brief's backward-compat intent."], "unresolved_issues": ["topic.min_relevance_score in config/base/app.yaml is not wired into any report filtering (report.py still hardcodes a 0.3 default) — explicitly called out as out of scope in the brief, but noting since the field currently has no effect if changed."]}
```

## Deviations from earlier steps
- phase 1 dev: Left src/html/duckdb-query-tool*.html references to is_mcp_related untouched — those files aren't in the brief's in-scope list and query historical parquet/frontmatter data that may still use the old column name.
- phase 1 dev: Applied 3 minor python-review cleanups beyond the literal brief text (deduped the is_relevant/is_mcp_related fallback into a private _is_relevant() helper in src/stages/report.py, fixed a stale 'MCP relevance score' docstring in src/models/url_registry.py, and added a comment explaining the anthropic_client is_mcp_related fallback) — all within already-in-scope files and consistent with the brief's backward-compat intent.

## Phase 1 — check
- 2026-07-05T22:40:00+02:00
```json
{"verified": true, "evidence": "1) `uv run poe check` output: 'All checks passed!' (ruff) x2, then pytest '336 passed, 1 warning in 14.60s'. 2) config/base/app.yaml has topic: {name: 'Model Context Protocol (MCP)', description: 'MCP is a protocol for AI tool integration...', min_relevance_score: 0.3}. src/config/config_manager.py has `class TopicConfig(BaseModel): name: str; description: str; min_relevance_score: float = 0.3`, `AppConfig.topic: TopicConfig`, and `get_topic_config()`. `uv run onsp validate-config` succeeded: '✅ Configuration is valid', 5 search definitions found, all queries build successfully (nsp has no validate-config subcommand, onsp does, as expected). 3) config/base/prompts.yaml template now reads 'Analyze this article for relevance to {topic_name}.' / '{topic_description}' / '1. is_relevant (boolean): Is this article about {topic_name} or closely related topics?' / '2. relevance_score ... How relevant is this to {topic_name}?', and `topic_name`/`topic_description` are both listed in `variables:` with required: true. No stray literal braces. 4) Rendered prompt directly via ConfigManager (topic_name='Model Context Protocol (MCP)' present in output, no '{topic_' substring) AND via constructing the real `AnthropicEvaluator(settings)` (dummy Settings, no network call) and calling `_create_evaluation_prompt('dummy body content')`: TOPIC NAME IN OUTPUT: True, UNFILLED PLACEHOLDER: False. 5) `grep -rn is_mcp_related src/` returns only: src/html/duckdb-query-tool*.html (explicitly out-of-scope per deviation note, not .py), src/evaluation/anthropic_client.py:179-181 (fallback `data.get('is_relevant', data.get('is_mcp_related', False))` with explanatory comment), src/stages/report.py:31 (fallback inside a new `_is_relevant()` helper, used at both filter call sites lines 154 and 309), src/utils/url_registry.py:43-48 (parquet backward-compat column copy `if 'is_mcp_related' in df.columns and 'is_relevant' not in df.columns: df['is_relevant'] = df['is_mcp_related']`). 6) `ArticleEvaluation.is_relevant` (src/models/evaluation.py:14) and `URLEntry.is_relevant` (src/models/url_registry.py:24) confirmed, no is_mcp_related field remains on either model. Grepped src/utils/url_registry.py, src/evaluation/processor.py, src/stages/evaluate.py, src/cli/stage_commands.py: all producer writes use `is_relevant` exclusively (mark_evaluated param renamed, get_stats uses 'is_relevant' column, evaluate.py dict/markdown/stats all use is_relevant). git log confirms commit 18c228b 'Genericize MCP topic into config; rename is_mcp_related to is_relevant' is present; working tree clean except untracked cf/ scaffolding.", "deviations": ["Confirmed as documented in the brief's own 'Deviations from earlier steps' section: src/html/duckdb-query-tool*.html left untouched (out of scope, historical parquet data), and report.py fallback logic deduped into a private _is_relevant() helper plus two cosmetic docstring/comment fixes -- all verified present and consistent with backward-compat intent."], "issues": []}
```

## Phase 1 — review
- 2026-07-05T22:50:00+02:00
```json
{"findings": [{"description": "Prompt genericization is incomplete: in config/base/prompts.yaml the relevance_score instruction line still hardcodes the topic — `2. relevance_score (0.0-1.0): How relevant is this to {topic_name}? 0=unrelated, 1=directly about MCP`. Every other topic reference on that line was templated to {topic_name}, but `1=directly about MCP` was left. context.md line 46 explicitly required the relevance_score wording that mentions MCP to become {topic_name}. This is an accidental leftover (not the intentionally-kept key_topics BAD-examples list). It doesn't break str.format (it's plain text, not a stray brace), but it defeats the genericization goal for any non-MCP topic. Fix: `1=directly about {topic_name}`.", "file": "config/base/prompts.yaml", "severity": "medium"}, {"description": "Newly-added comment in src/evaluation/anthropic_client.py (~line 178) justifies the is_mcp_related fallback as guarding against a 'stale (cached) template that still asked for is_mcp_related'. Prompt caching does not change the model's output field names, so the stated rationale is inaccurate; the real reason for the fallback is a model still emitting the old key or an in-flight deploy. The fallback code itself is correct and intended — only the comment's explanation is misleading. Minor.", "file": "src/evaluation/anthropic_client.py", "severity": "low"}], "dropped": ["Dead config knob topic.min_relevance_score (never read; report uses a hardcoded 0.3) — out of scope: context.md specifies adding this field to TopicConfig/app.yaml, and the brief + context.md 'Out of scope' explicitly defer wiring topic.min_relevance_score into the report filter plumbing.", "Rename not propagated to src/html/duckdb-query-tool*.html and notebooks/*.py parquet consumers — out of scope: the dev's documented deviation and brief task 3 explicitly exclude these files; they query historical parquet/frontmatter that may still use the old column name, and read-side fallbacks keep old data working.", "Suggestion to introduce a shared module-level constant for the legacy is_mcp_related field name across the three fallback sites — declined: adds an abstraction the repo's MVP principles treat as a finding, not a virtue; the three fallbacks handle genuinely different data shapes (dict vs pandas column) and are fine standing alone."], "deviations": []}
```

## Phase 1 — fix
- 2026-07-05T23:00:00+02:00
```json
{"commit_shas": ["00fb5de9b58771523a51a0513617f6802d2074cb"], "summary": "Fixed the leftover hardcoded MCP reference in prompts.yaml's relevance_score line and reworded the misleading is_mcp_related fallback comment in anthropic_client.py.", "deviations": [], "unresolved_issues": []}
```

## Phase 1 — check (post-fix)
- 2026-07-05T23:10:00+02:00
```json
{"verified": true, "evidence": "1) `uv run poe check` passed: ruff 'All checks passed!', ty 'All checks passed!', pytest '336 passed, 1 warning in 22.92s'.\n2) config/base/app.yaml has topic section: name='Model Context Protocol (MCP)', description=(verbatim old prompt text), min_relevance_score=0.3. src/config/config_manager.py has `class TopicConfig(BaseModel)` with name/description/min_relevance_score=0.3, and `AppConfig` includes `topic: TopicConfig`; a `get_topic_config()` convenience method exists (line 234). `uv run onsp validate-config` succeeded: '✅ Configuration is valid', '📊 Found 5 search definitions', all query builders succeeded (nsp has no validate-config subcommand, brief allows either).\n3) config/base/prompts.yaml template: line1 'Analyze this article for relevance to {topic_name}.', body '{topic_description}', item1 'is_relevant (boolean): Is this article about {topic_name} or closely related topics?', item2 'relevance_score (0.0-1.0): How relevant is this to {topic_name}? 0=unrelated, 1=directly about {topic_name}' (post-review fix applied - no longer hardcodes 'directly about MCP'). variables: list includes topic_name (required:true) and topic_description (required:true). No literal stray braces present.\n4) Rendered prompt via AnthropicEvaluator(_create_evaluation_prompt) with dummy settings (no API call made, only client construction): output contains 'Model Context Protocol (MCP)' (True) and contains no unfilled '{topic_' placeholder (False). Snippet confirmed topic substituted throughout.\n5) `grep -rn is_mcp_related src/` returns only: src/evaluation/anthropic_client.py (fallback read + accurate comment, no more 'prompt caching' text, mentions 'in-flight deploy'), src/utils/url_registry.py (backward-compat column copy block), src/stages/report.py (deduped into `_is_relevant()` helper used at lines 154 and 309), and src/html/duckdb-query-tool*.html (documented as out-of-scope deviation). ArticleEvaluation.is_relevant and URLEntry.is_relevant confirmed present via grep; no stale 'MCP relevance score' docstring found in url_registry.py.\n6) src/stages/report.py: `_is_relevant(evaluation)` = `evaluation.get('is_relevant', evaluation.get('is_mcp_related', False))`, used at both filter call sites. src/utils/url_registry.py: backward-compat block copies old is_mcp_related column to is_relevant when is_relevant absent (lines 43-48).\nWorking tree left clean by this check (git status --short shows only untracked cf/ directory, no modifications).", "deviations": ["Confirmed as documented: src/html/duckdb-query-tool*.html retain is_mcp_related references untouched (out of brief's in-scope list, historical data compat) - verified via grep, matches documented deviation.", "Confirmed as documented: report.py fallback deduped into private _is_relevant() helper; url_registry.py docstring fix; anthropic_client fallback comment added - all verified present and consistent with brief intent.", "Confirmed post-review fix applied: prompts.yaml relevance_score line now reads '1=directly about {topic_name}' instead of hardcoded MCP.", "Confirmed post-review fix applied: anthropic_client.py comment near is_mcp_related fallback (line ~178-180) no longer mentions prompt caching; now gives accurate rationale ('in-flight deploy' / model not yet updated)."], "issues": []}
```

## Summary
- 2026-07-05T23:15:00+02:00 · cml run complete
- phases: 1
- commits: 18c228b, 00fb5de9b58771523a51a0513617f6802d2074cb
- all checks verified: yes
- unresolved: 0
